# Login page background image

The login page uses **`image_bg.jpg`** in this folder as the blurred background behind the card.

- File: `public/images/image_bg.jpg`
- If the file is missing or fails to load, a neutral gray gradient is shown instead.
